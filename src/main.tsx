import React from "react";
import ReactDOM from "react-dom/client";
import App from "./step/5-email-send-button/App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
