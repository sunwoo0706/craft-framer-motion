import React from "react";
import ReactDOM from "react-dom/client";
import App from "./step/4-app-store-clone-animation/App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
